# Aaradhana V — Portfolio

## Run locally
Open `index.html` in a browser.

## Publish
Upload the entire `aaradhana_portfolio` folder to a static hosting service such as GitHub Pages, Netlify, or Vercel.

## Notes
- Certificate images are stored in `assets/`.
- Replace the two project descriptions with real project details once the projects are actually built.
- GitHub and LinkedIn buttons already point to the supplied profiles.
